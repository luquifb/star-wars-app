import { FilmSearcherPipe } from './film-searcher.pipe';

describe('FilmSearcherPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmSearcherPipe();
    expect(pipe).toBeTruthy();
  });
});
